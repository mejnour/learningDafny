method Find(a: array<int>, key: int) returns (index: int)
    ensures 0 <= index ==> index < a.Length && a[index] == key
{
    // if a[index] == key

    // index := -1;
    // if a.Length == 0 then {
    //     index := index + 1;
    //     a[index] := key;
    // } else {
    //     index := index + 1;
    //     a[index] := key;
    // }
}